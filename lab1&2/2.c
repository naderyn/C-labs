#include <stdio.h>
#include <stdlib.h>

int main() {
    char ch;

    printf("Enter the Character:");
    scanf("%c", & ch);

    printf("\nThe ASCII Value of %c is %d",ch,ch);

    return 0;
}
